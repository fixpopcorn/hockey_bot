# 🏒👕 Hockey Jersey Bot

Telegram-бот для учёта хоккейной формы в команде.

Бот позволяет легко отслеживать, кто и когда взял комплект формы, кто хранит его сейчас, а также вести историю всех действий.

---

## ✨ Возможности

### 🧥 Получение комплекта формы
Пользователь может выбрать один из комплектов:

- Тренировочный  
- Тёмный игровой  
- Белый игровой  

Перед записью бот:
- показывает текущего держателя (если есть),  
- просит подтверждение,  
- автоматически закрывает предыдущие активные записи,  
- назначает комплект новому участнику.

Это гарантирует, что комплект всегда находится **строго у одного человека**.

---

### 📄 История записей
Через меню можно:
- просмотреть до 50 последних записей,  
- узнать, какие записи активны,  
- удалить любую запись (с подтверждением),  
- восстановить статус комплекта по предыдущей записи.

---

### 🧑‍🤝‍🧑 Проверка текущих держателей
Бот показывает:
- кто держит выбранный комплект,  
- время получения,  
- свободен ли комплект.

---


## 🧩 Структура хранения данных

База данных хранится в SQLite и содержит следующие поля:

| Поле         | Описание                               |
|--------------|-----------------------------------------|
| `id`         | Уникальный идентификатор записи         |
| `user`       | Username или имя пользователя           |
| `uniform_type` | Тип комплекта формы                     |
| `timestamp`  | Время, когда комплект был взят (ISO)    |
| `active`     | Флаг активной записи (1 — текущий держатель) |

---

## 🛠️ Установка и запуск

### 🐳 Запуск через Docker (рекомендуется)

**1. Клонировать репозиторий**
```bash
git clone git@github.com:fixpopcorn/hockey_bot.git
cd hockey_bot
```

**2. Создать файл `.env`**
```env
BOT_TOKEN=ваш_токен_бота
```

**3. Запустить бота**
```bash
docker compose up -d --build
```

**Управление контейнером**
- Логи:
```bash
docker compose logs -f hockey_bot
```
- Остановить:
```bash
docker compose down
```

---

### 🐍 Локальный запуск (без Docker)

**1. Создать виртуальное окружение**
```bash
python -m venv venv
source venv/bin/activate
# Windows
venv\Scripts\activate
```

**2. Установить зависимости**
```bash
pip install -r requirements.txt
```

**3. Настроить `.env`**
```env
BOT_TOKEN=ваш_токен_бота
```

**4. Запустить**
```bash
python bot.py
```

---

## 📂 Структура проекта

```
bot.py               — запуск приложения
handlers.py          — обработка команд и callback-кнопок
keyboards.py         — inline-клавиатуры
db.py                — работа с SQLite и логикой статусов форм
docker-compose.yml   — деплой через Docker
Dockerfile
requirements.txt
```

---

## 💻 Технологии

- Python 3.11+
- python-telegram-bot (v20+)
- SQLite3
- Docker & Docker Compose

---

## 🎯 Рекомендации

- Папка `./data` должна быть доступна для записи контейнера.
- При первом запуске база создаётся автоматически.
- Команды можно настроить через @BotFather.

---